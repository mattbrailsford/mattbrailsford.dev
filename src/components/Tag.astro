---
export type Props = { tag: string }
const { tag } = Astro.props;
---
<a href={`/tag/${tag}`} class="inline-block px-2 px-4 rounded bg-gray-100 hover:bg-pink-500 hover:text-white transition-colors">#{tag}</a>